#include <stdio.h>

/**
 *main - Print a string using one of the c programming buitl in function puts.
 *Return: Return zero (success) in betty style as per the instruction.
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
