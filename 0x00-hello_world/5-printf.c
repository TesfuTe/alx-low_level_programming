#include <stdio.h>

/**
 *main - Print a string using one of the c programming built in function printf.
 *Return - The program should return 0 (success) as per the instruction.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
